```@meta
CurrentModule = ConsensusFitting
```

# ConsensusFitting

This package implements robust fitting algorithms to find best-fit models in the presence of outliers. Documentation for each algorithm can be found in separate pages in the sidebar.

```@index
```